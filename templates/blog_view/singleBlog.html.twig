{# empty Twig template #}

{% extends "base.html.twig"%}

{% block body%}

    <div class="container my-5">
        <div class="card border-none border-dark shadow">
            <div class="card-body">
                <div class="card shadow p-3 bg-dark">
                    <div class="row"> 
                        <div class="col-10">
                            <h3 class="text-bold text-white">{{blog.id}} : {{blog.blogTitle}}</h3>
                        </div>
                        <div class="col-2">
                            <a href="/blog"><i class="fa fa-arrow-left float-right mt-2 text-white"></i></a>
                        </div>
                    </div>
                </div>
                <div class="p-3">
                    <h1>Id : </h1>
                    <p class=" h5">Author : {{blog.Author}}</p>
                    <h5>{{blog.blogDescription}}</h5>            
                </div>

            </div>
        </div>
    </div>

{% endblock %}
